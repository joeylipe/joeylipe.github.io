source "https://rubygems.org"

# This gem is required for GitHub Pages to build your site
gem "github-pages", group: :jekyll_plugins

# This allows GitHub to go fetch the "Minimal Mistakes" theme remotely
gem "jekyll-remote-theme"

# Optional but highly recommended: speeds up the site build process
gem "jekyll-include-cache"
